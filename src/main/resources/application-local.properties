# 1. Local Database Connection (Already updated for your Mac)
spring.datasource.url=jdbc:postgresql://localhost:5432/gym_exercises
spring.datasource.username=davidsegoviano
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update

# 2. Security Settings (Moved from original properties)
application.security.jwt.secret-key=${JWT_SECRET_KEY}
application.security.jwt.expiration=86400000

# 3. Hibernate Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect